[project]
name = "stock-expert"
version = "0.1.0"
description = "CLI-based BIST intraday stock advisor MVP"
requires-python = ">=3.11"

[project.scripts]
stocks = "stock_expert.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
